
    /*
    else {
        
        //float edgesShiftX = onePixelSizeX / iScale;
        //float edgesShiftY = onePixelSizeY;
        
        // uv gets scaled by iScale?
        if (uv.x >= edgeUV){//&& uv.x <= 1.0 ) {
            gl_FragColor = vec4(0.0, 0.0, 1.0, 1.0);
        }
        else {
            gl_FragColor = texture2D(tex0, vec2(uv.x, uv.y));
        }
        */
        
        /*
        if (uv.x >= 1.0 - onePixelSizeX) {
            gl_FragColor = vec4(uv.x,0.0, 0.0, 1.0);
            //gl_FragColor = texture2D(tex0, vec2(uv.x, uv.y));
        }
    }
    */